server:
  port: 0

spring:
  application:
    name: sms-management-service
  profiles:
    active: test

twilio:
  accountSid: ${TEST_ACCOUNT_SID:ACXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX}
  authToken: ${TEST_AUTH_TOKEN:your_auth_token}
  trialNumber: "+15005550006"
  pollForStatus: true
  service:
    schedulingSMSSid: SCHEDULING_SID
    bulkSMSSid: SERVICE_SID

app:
  callback:
    url: https://ec46-216-209-46-31.ngrok-free.app/api/v1/callback
    endpoints:
      smsStatus: /sms-status
      health: /health
